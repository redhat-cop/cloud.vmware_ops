- hosts: localhost
  gather_facts: no
  collections:
    - community.general
  vars_files:
    - vars.yml

  tasks:
    - name: Vcsim
      ansible.builtin.import_role:
        name: prepare_soap

    - name: Import provision VM role
      ansible.builtin.import_role:
        name: provision_vm_test
